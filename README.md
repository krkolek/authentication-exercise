## Requirements
* Python 2.7

## Instalation
Go to project directory and then:
`pip install -r requirements.txt`
`python manage.py migrate`

## Endpoints:
#### **POST** */register*

#### **POST** */login*
